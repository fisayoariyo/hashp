# Optional override for direct API calls (skips the /api same-origin proxy).
# VITE_CROPEX_API_BASE_URL=https://hashmaramala-production.up.railway.app
#
# By default the app calls /api/... on the same host (Vercel rewrite + Vite dev proxy),
# which avoids browser CORS blocks on preview/custom domains.
